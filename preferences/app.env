# Number of frames cached ahead of and behind the current frame.
FRAME_CACHE_RADIUS=125

# Maximum seconds for each extracted audio snippet.
AUDIO_SAMPLE_SECONDS=20

# Number of sample points used to extract snippets across the full video.
# The extractor always uses the start and end, with evenly spaced points in between.
AUDIO_SAMPLE_POINTS=3
